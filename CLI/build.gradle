/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.9.1/userguide/building_java_projects.html
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
    id 'java'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Use JUnit test framework.
    testImplementation 'junit:junit:4.13'

    // This dependency is used by the application.
    compile 'com.google.guava:guava:29.0-jre'
    compile 'org.apache.httpcomponents.client5:httpclient5:5.1.1'
    compile 'org.slf4j:slf4j-simple:1.7.32'
}

application {
    // Define the main class for the application.
    mainClass = 'async.client.App'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'async.client.App'
        )
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}